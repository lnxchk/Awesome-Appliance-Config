---
driver:
  name: docker

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.4
suites:
  - name: default
    run_list:
      - recipe[mysql::server]
      - recipe[python::default]
      - recipe[aar::default]
    attributes:
